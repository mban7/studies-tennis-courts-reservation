[project]
name = "tennis-courts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["django", "ruff", "python-dotenv", "psycopg2-binary", "djangorestframework", "drf_spectacular"]
